#ifndef ARITHMETIC_H
#define ARITHMETIC_H

#include <string.h>
#include <stdlib.h>
#include <stdio.h>

// string match
// match finsh return > 0 else return == -1
int KMP(const char *S, const char *T);


#endif
